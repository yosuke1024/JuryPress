name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          
      - name: Install Dependencies
        run: npm ci
        
      - name: Typecheck
        run: npm run typecheck
        
      - name: Run Unit Tests
        run: npm run test:unit
        
      - name: Build
        env:
          JURYPRESS_DATA_MODE: fixture
        run: npm run build

      - name: Verify Fail Closed without Content Root
        run: |
          if JURYPRESS_DATA_MODE=production npm run build; then
            echo "Error: Build succeeded in production mode without JURYPRESS_CONTENT_ROOT. Fail Closed failed."
            exit 1
          else
            echo "Success: Build correctly failed in production mode without JURYPRESS_CONTENT_ROOT."
          fi

      - name: Verify Path Traversal Prevention
        run: |
          if JURYPRESS_DATA_MODE=production JURYPRESS_SITE_URL=https://pixapps.ai JURYPRESS_CONTENT_ROOT="../etc" npm run build; then
            echo "Error: Build succeeded with traversal path. Traversal check failed."
            exit 1
          else
            echo "Success: Path traversal correctly blocked."
          fi

      - name: Verify Fixture Contamination Prevention
        run: |
          if JURYPRESS_DATA_MODE=production JURYPRESS_SITE_URL=https://pixapps.ai JURYPRESS_CONTENT_ROOT="./tests/fixtures" npm run build; then
            echo "Error: Build succeeded with fixture data in production mode. Contamination check failed."
            exit 1
          else
            echo "Success: Fixture data contamination correctly blocked."
          fi
        
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium
        
      - name: Run E2E Tests
        run: npm run test:e2e
